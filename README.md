# Плагин AI-Chat 🤖

Плагин `ai-chat` добавляет в Minecraft умного ИИ-бота с поддержкой провайдеров **Google Gemini** и **OpenRouter**. Поддерживает триггерные слова, персональные режимы (системные промпты), историю диалога, прокси, кулдауны и права доступа.

## Как он работает

1. Игрок пишет в чате сообщение, **начинающееся** с триггерного слова (напр. `бот, как дела?`).
2. Проверки: права, кулдаун, тип чата, не обрабатывается (если уже думает).
3. Определяется **режим игрока** из БД (`ai:mode:username`, дефолт: `default`).
4. Загружается системный промпт из `config/prompts.json`.
5. Запрос к AI (с историей, если включено). Ответ очищается от эмодзи/иероглифов, обрезается до 250 символов.
6. Ответ отправляется **в тот же чат** игроку.

## Пример действия

**Игрок (в клан-чате):**
> `бот` какой самый прочный блок в майне?

**Бот отвечает (в клан-чат):**
> Обсидиан — самый прочный в выживании. Коренная порода/барьер не ломаются!

## Какие команды/функции добавляет

### `/gptmode` (`/gptm`, `/mode`)
Управление **режимами ИИ** (требует права `admin.gpt`).

| Использование | Описание | Пример |
|---------------|----------|--------|
| `/gptmode` | Показать **свой** режим | `Ваш режим: &edefault` |
| `/gptmode <игрок>` | Показать режим **игрока** | `/gptmode Steve` → `Режим &eSteve: &estrict` |
| `/gptmode <режим>` | Сменить **свой** режим | `/gptmode fun` → `Ваш режим изменён на &efun` |
| `/gptmode <игрок> <режим>` | Сменить режим **игрока** | `/gptmode Steve strict` |

## Режимы ИИ

- Хранятся в БД: `ai:mode:<username.lowercase>` (дефолт: `default`).
- Определяют **системный промпт** из `config/prompts.json`.

**Пример `config/prompts.json`:**
```json
{
  "default": "Ты — полезный ИИ-бот в Minecraft. Сейчас {time}, игрок {username} в чате {chatType}. Отвечай коротко и дружелюбно.",
  "strict": "Отвечай строго, без шуток. Только факты.",
  "fun": "Будь весёлым и с юмором! 😄"
}
```

**Плейсхолдеры в промптах:** `{username}`, `{chatType}`, `{time}`, `{date}`, `{datetime}`.

## Все существующие настройки

### Панель Botpanel (package.json)
| Настройка | Тип | Описание | Значение по умолчанию |
|-----------|-----|----------|-----------------------|
| `proxy` | `proxy` | Прокси для AI-запросов | `{enabled: false}` |
| `aiProviderConfig` | `json_file` | Конфиг AI (`config.json`) | `config.json` |
| `prompts` | `json_file` | Промпты режимов | `config/prompts.json` |
| `allowedChatTypes` | `string[]` | Чаты: `chat`, `clan`, `global` и т.д. | `['clan', 'chat']` |
| `thinkingMessage` | `string` | &quot;ИИ думае��...&quot; | `Думаю над вашим вопросом...` |
| `cooldownMessage` | `string` | Кулдаун: `{timeLeft}` | `Подождите еще {timeLeft} сек.` |
| `noPromptMessage` | `string` | На пустой запрос | `Я вас слушаю. Какой у вас вопрос?` |
| `useHistory` | `boolean` | История диалога | `true` |
| `clearBadSymbols` | `boolean` | Удалять эмодзи/иероглифы | `true` |

### Файл `config.json` (AI-провайдеры)
```json
{
  "provider": "google",
  "triggerWords": ["бот", "bot", "ии"],
  "cooldown": 10,
  "providers": {
    "openrouter": {
      "apiKey": "sk-or-...",
      "model": "google/gemini-2.5-flash",
      "maxTokens": 100,
      "temperature": 0.7
    },
    "google": {
      "apiKeys": ["AIzaSy..."],
      "model": "gemini-2.5-flash",
      "maxTokens": 150,
      "temperature": 0.8
    }
  }
}
```

**Ключи API:**
- [Google AI Studio](https://aistudio.google.com/app/apikey)
- [OpenRouter](https://openrouter.ai/keys)

## Права доступа
| Право | Описание |
|-------|----------|
| `user.ai` | Доступ к ИИ-чату |
| `user.ainocd` | ИИ без кулдауна |
| `admin.gpt` | `/gptmode` (все действия) |

**Авто-добавление:**
- `Member` (если `clan-role-manager`): `user.ai`
- `Admin`: `user.ainocd`, `admin.gpt`


## Информация для разработчиков
- Событие: `chat:message` (из parser-keksik).
- Хранилище: `ai:mode:<username>` для режимов.
- AI-клиенты: адаптеры для OpenRouterClient/GeminiClient с прокси.
- Utils: `utils/textUtils.js` — очистка текста.